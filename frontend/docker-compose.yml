services:
  frontend:
    build:
      context: .
      args:
        - VITE_API_BASE_URL=http://localhost:5050
    container_name: hr_agent_frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://localhost:5050
    restart: always
